<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/rabbit
     http://www.springframework.org/schema/rabbit/spring-rabbit-1.3.xsd
     http://www.springframework.org/schema/context
     http://www.springframework.org/schema/context/spring-context.xsd">
    <description>Message AMQP Configuration</description>

    <context:property-placeholder ignore-unresolvable="true" location="classpath:/properties/amqp.properties2"/>

    <rabbit:connection-factory id="connectionFactory" host="${rabbitmq.host}" port="${rabbitmq.post}"/>
    <!--创建一个管理组件，它会自动创建下面声明的queue、exchange、bingding-->
    <rabbit:admin connection-factory="connectionFactory"/>

    <rabbit:queue id="defaultQueue" name="com.sane.default"/>
    <rabbit:queue id="1" name="com.sane.school" />
    <rabbit:queue id="2" name="com.sane.hospital" />

    <rabbit:fanout-exchange name="com.sane.fanout" >
        <rabbit:bindings>
            <rabbit:binding queue="com.sane.hospital"></rabbit:binding>
            <rabbit:binding queue="com.sane.school" ></rabbit:binding>
        </rabbit:bindings>
    </rabbit:fanout-exchange>

    <rabbit:template id="rabbitTemplate" connection-factory="connectionFactory" />
</beans>